import { TabButton, TabWrapper } from "./style"

const Tab = () => {
  return (
    <TabWrapper>
      <TabButton>Swap</TabButton>
      <TabButton>Liquidity</TabButton>
    </TabWrapper>
  )
}

export default Tab
